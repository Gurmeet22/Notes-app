# Notes-app
A CLI application notebook

Notes are sored in local directory in JSON format.
User can add a new note, delete existing note, read existing note and list all the notes.

Node.js application - 
For more help run 'node app.js --help'
Install all the dependencies by running 'npm install'
